name: Deploy Dist
on: [push, workflow_dispatch]
jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              run: |
                echo: "Cloning Repo..."
                echo: "${{toJson(github)}}"
                git clone https://github.com/${{github.repository}}.git